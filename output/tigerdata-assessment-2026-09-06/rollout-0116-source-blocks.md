# Audited uncatalogued PICS head evidence

The user approved production rollout and subsequently approved retaining uncatalogued head IDs as audited source-blocked work outside the known-catalog manifest. This supplements applied SQL 0111; it does not replace any archived source record or mark an unavailable app recovered.

SQL 0116 adds `ops.pics_successor_source_blocks` and updates preparation, retargeting and activation. Each selected uncatalogued source position retains its batch foreign key, source index, app ID, exact change number and token flag. Duplicate source positions remain distinct. Retargeting retains the earlier head's records. There are at most 1,000 source-block positions per selected head; larger heads fail atomically and require a smaller fresh boundary. The archive verifier still verifies every child, including these records. Activation rechecks exact source-block coverage before advancing the cursor.

Risk: medium for the additive schema and guarded function replacement; high for the subsequent audited checkpoint activation as already documented. Tradeoff: small additional audit writes/storage and unresolved records outside the catalog manifest. These records have no automatic completion or retry path; later source/catalog evidence requires explicit reconciliation. They do not fabricate catalog rows, bypass access tokens, mutate retained primary work, or claim historical gap recovery. Existing 9,183 uncatalogued primary work rows remain unchanged and included in predecessor review.

Validation: `implementation-source-block-tests.py` runs real SQL against generated local PostgreSQL data. It verifies exact duplicate/token/watermark retention, missing/corrupt audit rejection, atomic size bounds, retained retarget evidence, unchanged old audits and primary work, bounded resumable staging, live/pause behavior, current-run settlement, and rollback. It uses no production data. See `implementation-source-block-results.json` for timings; local results are not production capacity gains.

Rollout: snapshot the three current production definitions and verify they match SQL 0111, apply hash-pinned SQL 0116 with stop-on-error, confirm commit and function/table definitions. Then select a fresh complete head, verify both archives read-only, review prior dead letters/source-block fingerprint, prepare/stage/seal in bounded transactions, and reverify evidence immediately before separately recorded activation. Keep catch-up paused and the automatic feeder off through live-only validation and required capacity gates.

Rollback: `rollback-0116.sql` restores the exact conservative 0111 functions while retaining the new table, source records, helper, current cursor, primary work and all archives. Pause catch-up before any runtime rollback. An activated cursor is repaired forward; never use the old destructive checkpoint rollback. Unapplied staging can be abandoned with its existing audited function. No rollback deletes accepted evidence.
