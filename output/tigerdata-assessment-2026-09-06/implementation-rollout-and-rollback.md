# Efficiency and reliability rollout / rollback

Updated September 7 UTC. **Only the separately approved memory-limit workflow was published in PR #124; all schema, runtime, replay and recovery changes below remain unapplied production candidates.** This document does not authorize additional production changes. Local implementation, isolated tests, and bounded production reads are authorized. The checklist records measured results and outstanding work. All proposed initial-scope architecture choices are approved and implemented, including bounded PICS pooling, demo peak reuse, YouTube incremental calculations with timestamp writes, and CCU progress checkpoints. T6 follows initial rollout as approved. Implementation approval does not authorize deployment, production writes, replay, resizing or embedding funding. Sentry is excluded from the measurement plan at the user's request; no replacement APM purchase or installation is required.

## Release boundaries and dependency order

The first production packet, `rollout-first-memory-limits.md`, was approved and merged as PR #124 (`de27ce0471bb21f5ad70e35cad5dd09285b20c0e`). It bounds refresh session memory without installing the gate/schema or adding recovery load. Observation of its next ordinary scheduled run remains pending; the published workflow alone does not prove the memory failures are resolved. Its separate patch is necessary because the full local workflow already depends on 0109.

Keep the user's Opportunities/newsletter changes separate. Build an explicit release manifest from only the efficiency changes; do not stage the whole working tree or the whole output directory. Record the reviewed commit/image SHA, prior deployed SHA, database definitions, workflow files and sanitized configuration before each release. The workflow release SHA above is recorded; no PICS candidate has been deployed. The old Railway service reports no source commit, so retain its deployment identity without inventing a rollback SHA.

Six review bundles are prepared in `review-bundles/`, with exact file lists, baseline SHA and patch SHA-256 values in `manifest.json`. Each passes an apply check against baseline `eae97f14b83a443a1aeb2dcf41b28bb476c94dd5`. The original full-bundle checks used pre-#124 main `8f4190b9160ea9ce75ca422cb092cb40753a2e51`. After #124, exclude `.github/workflows/unreleased-projection-refresh.yml` from bundle 03 and apply `rollout-refresh-gate-after-memory.patch` for that path. The current post-#124 check is recorded in `pics-asap-release-preparation.json`; preserve the unrelated main README content. The shared opportunity repository patch contains only efficiency hunks; newsletter edits remain in the user's working tree. Bundle grouping is for review, not permission to deploy all changes together. The database writer bundle includes demo code, so **0112 and 0114 must exist before deploying that whole bundle**; use the per-step hunk boundary if releasing only taxonomy/CCU first. Install the **gate helper only**, deploy gate-before-fence opportunity callers and drain old processes, then apply full 0109 as detailed below. Full 0109 precedes SQL 0110/PICS gates; SQL 0111 precedes successor-aware PICS code.

| Step | Concrete changes | Risk / tradeoff | Required rollback |
|---|---|---|---|
| 1: write amplification | SQL 0105 transition invalidation and 0106 storefront differences; guarded taxonomy and daily/latest CCU writes | Medium: trigger/authority semantics. Retain all observations, provenance, timestamps, equal-peak source/date changes and true deletions. | Guarded `rollback-0105.sql` / `rollback-0106.sql`; revert only the corresponding writer edits. Keep all data/revisions. |
| 2: queue cost | SQL 0107 concurrent expired-claim index, 0108 review claims; opportunity due/expired branches and duplicate-readiness exclusion | Medium: quotas, ordering, leases. Extra index adds bytes and maintenance; review lock/WAL savings do not establish CPU savings. | `rollback-0108.sql`; revert claim/readiness code. Retain the valid index initially; optional approved `rollback-0107.sql` later. |
| 3: bounded heavy phases | SQL 0109 wraps both native refresh procedures. Unreleased workflow uses 32 MiB temp buffers, 16 MiB work memory, hash multiplier 1, no parallel gather workers; opportunity fallback obtains the gate before its source fence | Medium: refreshes may wait up to 120 seconds. Source fencing retains the existing write-blocking scope. Lower memory may increase runtime/I/O. | Restore corresponding workflow/coordinator code before `rollback-0109.sql`. Keep the additive gate function. Never mix ungated heavy procedures with active recovery. |
| 4: unreleased query | SQL 0110 builds a separate MV pair; then separately reviewed `rollout-unreleased-swap.sql` renames serving pairs atomically | Medium/high: temporary duplicate storage, build work, schema/ACL and name-binding risk. Local parity is not production OOM proof. | Original MV identities remain. `rollback-unreleased-swap.sql` restores names; assess retained pair freshness first. No drop of either pair during the canary. |
| 5: ordinary collectors | Tag preclaim cooldown/owned deferral; null-review failures; per-25 CCU persistence; strict SteamSpy terminal handling; distinct histogram IDs; application names; targeted builds | Medium: useful retained/recovered work adds transactions/storage. CCU hard kill can lose a buffer of at most 24 valid samples plus in-flight requests. SteamSpy paced retries can take longer. Builds can start worker phases earlier. | Revert corresponding code/build changes; retain every committed observation/archive. No replay of completed jobs solely to test rollback. |
| 6: demo/cohort | SQL 0112 evaluation state/read view and 0114 exact peak reuse; changed demo assignment writes; exact feature buffer cache ≤16 MiB | Medium: derived-state dependencies and compatibility read contract. Cold/expired peak passes can take twice as long; source invalidation adds CPU/WAL. Cache adds retained plus temporary decoding memory. | Disable peak reuse with the guarded rollback, then revert demo writer only after reconciling evaluated timestamps as below; retain state/view/history. Revert cohort cache code without changing persistent TTL. |
| 7: YouTube bounded inputs | Keep complete daily rebuild/correction scope; use exact window snapshots plus each video's preceding anchor | Medium: adds indexed probes. Local 22-column parity includes late/negative corrections; Timescale production timing is not measured. | Revert only `needed_snapshots`/`snapshot_intervals` change. Daily transaction boundaries, deletes, observations and scheduling stay intact. |
| 7a: YouTube incremental publication | SQL 0115 plus incremental writer; all 25 production columns, timestamp/default writes and complete correction windows retained | Medium: source-trigger CPU/WAL and derived ledger storage; full-dirty runs can be slower. Independent net-benefit canary required. | Disable reuse for correctness; use the tested gated bounded-full fallback and guarded tracking disable for performance, as detailed below. Retain all source/output data. |
| 7b: daily price tuple suppression | SQL 0113 guards only identical daily price/discount values; app updated-at, price-fetch timestamps, date changes and returned app count remain | Low/medium: comparisons still lock conflict rows; changed-price/new-day runs can be slower. No currency/cadence or freshness change. | `rollback-0113.sql` checks the candidate function body and restores the exact reviewed original. No data rewrite or history removal. |
| 8: PICS runtime + recovery schema | SQL 0111; progress/deadlines/offload, periodic leases, relationship differences, empty batches, queue metrics, shared promotion gates bounded role pools, and the opt-in automatic successor feeder | High during recovery: additional useful ingestion, audit storage/WAL, persistent backend memory and Steam request mix. One Steam service/session/governor only; six database connections at current concurrency three. | Before activation, retain schema and revert reviewed runtime if needed. After activation, retain successor pause/current-run audit scoping even on code rollback; use the supplied compatibility image/patch and forward recovery. |
| 9: bounded repair campaigns | First a separate 12-row tag canary; later PICS stage/activation/enqueue and canaries | High: restored load and current-state recovery after a documented gap. Missing historical versions cannot be reconstructed. | Tag exact-after-image rollback only while untouched; otherwise retain results and stop expanding. PICS stop new catch-up claims, drain owned work, repair forward. Never run the old destructive checkpoint rollback. |

All SQL files are unapplied. Apply each approved step separately, with stop-on-error enabled and a recorded transaction result. Use an existing secure libpq environment (`PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSWORD`, SSL); commands do not require putting a URL in logs. Example after approval: `/opt/homebrew/opt/libpq/bin/psql -X -v ON_ERROR_STOP=1 -f <reviewed-file.sql>`. Session memory settings apply to the dedicated session, not the whole service.

0107 uses `CREATE INDEX CONCURRENTLY` and must run outside a transaction. Check `pg_index.indisvalid` and `indisready`, definition/predicate/order and size afterward. If a failed prior build left an invalid same-named index, stop: `IF NOT EXISTS` does not repair it. Its removal/rebuild requires a separately reviewed action. Do not automatically rerun through that state.

## Baseline and capacity acceptance

Keep Tiger at **0.5 CPU / 2 GiB**, Railway resources unchanged, embeddings unfunded. Do not spend modelled compression savings as available recovery capacity.

Before recovery, obtain a 24-hour matched baseline after ordinary efficiency fixes, including one successful unreleased refresh. Use existing telemetry and low-volume worker phase logs; label the assessment/verifier's own queries. Capture:

- Provider CPU versus allocated CPU, actual service memory definition/bytes, OOM/oom_guard events, temp/WAL byte deltas and active/peak connections. PostgreSQL cache bytes and the OOM log's internal counter are not interchangeable with total service memory.
- Ordinary Storefront/CCU/reviews, demo bursts, histogram and YouTube phases; native apps/cohort refresh start/end; Railway opportunity work, downstream queues and request traffic. Current recorded GitHub overlap is seven worker steps and three families for 72 seconds, excluding other platforms. This is not a complete overlap cycle.
- Authenticated `/apps`, `/companies`, `/unreleased`, opportunity and representative query-api contracts, with the same filters/subjects and data windows. Use Chrome plus Vercel's real request logs and the bounded direct contract probe; Sentry is excluded. Report sample count, errors, median/range and cache/request type; report p95/p99 only with adequate samples. A sign-in redirect, demo chart, or Active CPU metric is not page latency. Avoid artificial high-rate load on production. See `implementation-browser-latency-baseline.md` for the measurement procedure without Sentry.
- Per-source freshness/coverage and observed successful samples. PICS: accepted cursor/response timestamps, live oldest-ready age, raw/archive/manifest parity, source-blocked versus runnable counts, lease losses, retries/dead letters, promotion/heartbeat/connection times and request counts by class.

Predeclare canary gates with the baseline. Proposed conservative gates, subject to metric calibration: any OOM or user-visible failure stops expansion immediately; Tiger CPU >70% of allocation for five minutes or memory >80% for three consecutive one-minute samples stops new catch-up; p95 or p99 >20% over matched baseline for two five-minute windows requires review (report absolute latency and ≥100 samples, otherwise use existing SLO/error alerts and do not call the percentile conclusive). Do not weaken an existing stricter SLO. Rising live oldest-ready age over three processing passes also stops catch-up expansion; the fixed 40-live ceiling was insufficient on five of seven sampled arrival days.

A canary must include a full normal daily overlap cycle: site traffic, Storefront + CCU + reviews, a demo burst, unreleased/apps/cohort refreshes, YouTube work and downstream opportunity processing. Absence of OOM while PICS is stalled proves nothing about restored load. Synthetic local CPU/bytes/timings are not production improvements.

On a stop signal: pause new successor catch-up in the database, let owned atomic work settle, keep raw intake and live work running, and investigate. Record queued/settled counts and CPU/memory/error recovery before another stage. Do not restart a responsive source-blocked process in a loop.

## Unreleased stage / swap

1. Re-read exact original view definitions, ordered columns, owners, grants (including grant options), indexes and dependencies. The generated stage/swap guards must match. Rebuild the artifacts and review any drift instead of bypassing them.
2. After approval, complete the helper/caller/native sequence below and add the workflow gate to the already bounded memory configuration. Confirm ordinary refreshes can complete under the memory limits and measure duration/temp bytes. Do not dispatch an extra production refresh merely for a benchmark without approval.
3. Apply 0110 during a quiet approved heavy-phase window. It takes the shared mechanism exclusively and leaves serving names untouched. Measure physical duplicate bytes, peak memory, temp/WAL, elapsed time and query errors. It clones all 18 indexes, ownership, grants and comments.
4. Validate staged IDs/68 serving columns/filter counts and representative page sorting/filter queries at a coherent source time. Existing production materialization is stale, so direct equality with August's retained rows is not an appropriate current-data test. Local full-source equivalence is established; production checks must account for concurrent legitimate source changes and must not run an unbounded old full query that recreates the OOM.
5. Refresh the staged pair within five minutes of the swap. Review schema, valid indexes, ACLs and new dependencies. The swap guard refuses drift/unpopulated/stale candidates. Use the separately approved swap script with its short relation-lock timeout; on timeout, serving names remain unchanged.
6. After swap, verify authenticated page results/responsiveness and the next scheduled concurrent refresh. Retain both old MVs throughout the canary. Rollback restores their original relation identities, and now refuses stale retained rows (>5 minutes), changed retained definitions, access/column drift, invalid indexes and unexpected dependencies. If the old pair is too stale, obtain approval to refresh it safely under the same gate/memory bounds, or repair the new pair forward. Do not bypass the guard: name rollback cannot restore freshness. Local tests exercise access/dependency/staleness refusal and exact original-identity restoration.

## Gate installation across mixed runtime versions

The native wrapper and the opportunity caller must be staged together. Older opportunity callers take their source-table fence before calling the refresh procedure. If full 0109 were installed first, such a caller could hold that fence while waiting for the new global gate, delaying ingestion and reintroducing inconsistent lock ordering. This is a rollout risk derived from the exact old/new code; no production deadlock is claimed.

1. Under its own approval, apply `rollout-heavy-gate-helper-only.sql`. It installs the exact already-tested helper and leaves both native procedures unchanged. It refuses installation if the helper already exists; after an uncertain commit, inspect its definition/result rather than blindly retrying.
2. Deploy the reviewed opportunity caller that obtains the gate **before** acquiring the source fence, with its other release dependencies satisfied. Confirm the new runtime revision, drain old replicas and any old in-flight refresh/fence, and inspect active sessions before proceeding. Until all callers and native procedures participate, do not claim complete cross-platform coordination or enable recovery. A bounded runtime transition can delay evaluations; retain queued work and monitor freshness.
3. Apply full 0109 after old callers have drained. Its procedure hashes still guard the reviewed originals, and replacing the identical helper is safe. Add `rollout-refresh-gate-after-memory.patch` to the first memory-limits workflow release. Applying first-memory then this gate patch produces the exact tested full workflow. Do not apply the original bundle-03 workflow hunk on top of the first patch; exclude that path from the bundle and use this follow-up patch, or regenerate against the accepted commit.
4. Verify the native/coordinator/workflow phases actually take the same gate, then proceed to separately approved projection/PICS steps. On rollback, remove dependent recovery/gate callers as specified and restore guarded native definitions while retaining the additive helper and all data. Never treat an incompletely gated mixed fleet as safe recovery capacity.

`implementation-gate-rollout-sequence-results.json` proves on isolated PostgreSQL that the helper stage leaves both original procedures byte-equivalent, rejects repeat installation, permits the full migration afterward, and retains the helper while rollback restores the exact original procedure bodies. The prior multi-session fence test separately proves gate-before-fence ordering, ordinary source writes while waiting, and exported-snapshot correctness. These are local rollout/correctness tests, not production transition evidence.

## Demo evaluated-at compatibility

0112 is additive; existing candidate queries that do not select `updated_at` continue using the original table/indexes. The view `ops.demo_ccu_tier_assignments_evaluated` exposes the full row with evaluated-at `updated_at`, including unchanged ranking passes and later polling updates. A compact singleton stores the latest evaluated member set. Members leaving that set first persist their last evaluation time into their base row. Ranking comparisons, due selection, cadence, zero validation and observations remain unchanged.

Before reverting the new writer, stop starting a new demo ranking pass and let the active one finish. In an approved bounded transaction, materialize `view.updated_at` into base rows only where distinct, then deploy the old writer. Keep state/view so the prior evaluation remains inspectable. Resume the same cadence and verify the next ranking. This rollback writes up to the current demo set and has a cost; it never deletes observations or resets poll state. See `rollback-demo-evaluated-at.sql`.

## Tag repair canary

`rollout-tag-cooldown-canary.sql` proposes **12 exact rows** from the saved read-only manifest, all terminal with the exact circuit-open message and no owner. It preserves payload, trigger/source fields, prior error and observation pointers, clears terminal state, restores dirty work, grants one fresh retry budget and delays eligibility for 60 seconds. This is an audited new attempt budget; historical logs do not establish the exact number of real HTTP attempts for each row. Do not describe it as an exact historical refund.

Apply only after the cooldown fix is deployed and a normal cooldown/resume cycle is observed. The SQL checks complete before-images under row locks and aborts the whole proposal on drift. Save its committed JSON result as the rollback audit. No PICS replay during this canary. Verify actual HTTP attempts, success/empty/parser/source errors, no-attempt deferral, request budget, app readiness and downstream opportunity work. Do not immediately replay all 6,977 historical candidates.

`rollback-tag-cooldown-canary.sql` takes the committed JSON through psql's SQL-quoted `tag_repair_audit` variable and restores only rows exactly matching the repair's after-image. If any worker claim/new input/progress occurred, it aborts the whole rollback. Keep successful observations and use forward recovery instead. Any uncertain COMMIT response requires a read-only before/after reconciliation before retry; never blindly reset attempts again.

## PICS recovery sequence

### Preflight and fresh evidence

- Identify the genuine Railway service and deployed SHA/env by read-only inspection; leave the duplicate stopped. Preserve the original Steam owner/session/governor, existing product pass spacing (runtime 215 seconds), request pacing, retry limits and token handling. Do not add a second Steam collector for shadow capture.
- Re-read canonical cursor, primary work counts, July reconciliation identity/items, the three dead letters and uncatalogued source-blocked outcomes. The last verified canonical cursor is **37,773,873**; the old run is **54e2444b-2fc4-472a-b686-1173703f9212**. These are evidence, not reusable execution arguments. Apps 314/1000/8780 must remain honestly unresolved in July's audit.
- The targeted frozen-cursor R2 audit found 0/0/53 objects on August 4/5/6; first/last August 6 bodies are verified forced-full empty responses. No recoverable orphan was found in those paths. Expand exact relevant path inspection if new evidence points elsewhere; do not claim the whole archive has been exhausted.
- Complete SQL 0111 and the reviewed runtime deployment before any successor activation. Start with processing disabled and watchdog/borrowing disabled. A new durable poll can retain a fresh forced-full response without advancing the primary cursor. Record its archived gap batch and reported current head H.
- For fresh head evidence, change the **same genuine service** to `PICS_WORK_MODE=shadow`, a unique reviewed `PICS_INTAKE_STREAM_KEY`, `PICS_SHADOW_START_CHANGE_NUMBER=H`, `PICS_PROCESSING_ENABLED=false`; keep the same polling/request limits. Capture a complete non-primary response and its children/archive. Shadow capture continues during staging. A deployment handoff briefly interrupts collection; keep it short and account for it in retained intervals. Do not use July's old shadow response or invent H.
- A head with uncatalogued source IDs is rejected by this conservative path. Retain its archive and resolve its source evidence or choose a subsequent complete boundary that passes the guard. Do not fabricate catalog rows, remove child IDs, or clear token requirements to make activation pass. Existing uncatalogued source-blocked work stays retained and reviewed outside the known-catalog manifest.

### Prepare and stage (each write requires approval)

Run bounded SELECTs to capture `ops.pics_successor_review_state(previous_run_id)` as JSON and review every dead-letter disposition, status count and uncatalogued-work fingerprint. Then use the concrete function contract with freshly verified IDs:

```sql
SELECT ops.prepare_pics_successor_checkpoint(
  previous_run_id, expected_primary_cursor, proposed_head_cursor,
  archived_gap_batch_id, archived_head_batch_id, reviewed_previous_run_json,
  operator_identity, reason, explicit_previous_dead_letter_review_note
);
SELECT ops.stage_pics_successor_manifest(successor_run_id, 500);
SELECT ops.seal_pics_successor_manifest(successor_run_id);
```

Supply typed parameters through the database client; identifiers above are named placeholders, not SQL literals to copy blindly. Set a caller statement timeout of 15 seconds and lock timeout of 3 seconds for ordinary stage/enqueue calls. One committed call is one durable progress step; pause between calls and inspect workload gates. A busy heavy gate defers the call. No primary queue, readiness or cursor changes during staging.

If new apps arrive before activation, restart the bounded scan with `ops.stage_pics_successor_manifest(run_id,500,true)` once, then continue without `true` and reseal. It appends missing IDs without discarding existing records/indices. After a long staging pause, use `ops.retarget_pics_successor_head(run_id,new_head_batch_id,operator,reason)` to retain the manifest while reviewing a newer head. If predecessor outcomes changed, read the complete review again and call `ops.review_pics_successor_predecessor(run_id,review_json,operator,note)`. These actions are audited and do not advance the cursor. An unapplied run can be abandoned with `ops.abandon_pics_successor_staging`; its records remain retained.

### Verify and activate

Run from `services/pics-service`, using approved read-only Tiger and R2-read credentials:

```text
PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify_successor_evidence.py \
  --gap-batch-id <fresh-gap-uuid> --head-batch-id <fresh-head-uuid> \
  --output <review-directory>/pics-successor-evidence.json
```

The verifier checks raw object byte length/hash/content type, v2 headers and flags, source order, token bits, watermarks, item count and manifest against bounded retained DB children. It does not write to production. Reports expire after 15 minutes; head evidence must be within 30 minutes. If a bound is exceeded, stop and review the actual archive; do not silently truncate.

With the reviewed sealed count/hash/report and the primary restart ready, separately approve:

```sql
SELECT ops.activate_pics_successor_checkpoint(
  successor_run_id, reviewed_manifest_count, reviewed_manifest_sha256,
  verified_archive_report_json
);
```

Use a dedicated session with a 150-second caller statement timeout (covers the 120-second gate wait); short source-lock waits remain enforced. Activation rechecks source/cursor/review/catalog/evidence, records the old active run as `superseded_after_review`, retains its item dispositions, advances the canonical checkpoint, and leaves catch-up **paused**. It does not enqueue the manifest. Exact-argument retries are idempotent; a changed report is not permission to reapply blindly.

Immediately restore the same service to `PICS_WORK_MODE=durable`, stream `primary`, and verify it starts from the database cursor. Keep raw intake working; compare retained shadow intervals beyond the checkpoint with the primary continuation. If continuity cannot be proven or the source is forced-full again, pause expansion and repair forward. Never roll the cursor backward while discarding already accepted batches.

### Enqueue and processing canaries

Start by enabling normal processing with current runtime total/request/concurrency limits and no newly enqueued catch-up. After fresh-work health is measured, enqueue **at most 100** manifest members per approved call, initially one call. `ops.enqueue_pics_successor_chunk(run_id,100)` preserves work IDs, token flags and per-app watermarks and captures each work/readiness before-image; claimed/locked live work is skipped for a later call. It writes new readiness-pending markers only for linked members, leaving product rows available.

Resume catch-up with `ops.set_pics_successor_catchup_paused(run_id,false,operator,reason)` only for an approved canary. Pause uses the same function with `true`; it stops new catch-up claims without restarting raw intake. Live work linked to the successor remains claimable. Already owned promotions settle atomically; no destructive cancellation.

Keep `PICS_SUCCESSOR_FEEDER_ENABLED=false` during the initial live-only baseline and one manually bounded catch-up canary. The audited checkpoint remains paused until that canary is explicitly approved.

Proposed progression: zero catch-up baseline → up to 10 catch-up/pass → 20/pass only after matched capacity gates pass. Keep the existing combined app cap at or below 100 and downstream concurrency at or below the measured runtime 3. Do not increase Steam request frequency. The fixed 40 live slots are a temporary measurement stage, not a sustainable guarantee. If live lag grows, enable the approved live-borrowing flag within the existing combined cap and reserve catch-up capacity explicitly (proposed reserve 10); this slows recovery and may change token/request mix. Measure actual request classes and pass duration before selecting a long-term split.

After the initial canary and full normal overlap cycle meet the capacity gates, enable `PICS_SUCCESSOR_FEEDER_ENABLED=true` on the same worker. At a 10-app catch-up allocation it admits at most 10 members per processing pass with at most 20 open catch-up rows; limits remain capped at 100/200 even with borrowing. Delayed retries and owned claims consume the window. It uses durable item links to resume across restarts or an uncertain commit, and yields to waiting live work, the heavy-phase gate, intake contention, operator row locks and the persistent pause. This removes repeated manual enqueue commands without increasing the processing cap or Steam request frequency. Automatic admission adds a bounded transaction each enabled pass; measure it and the restored downstream work rather than assuming zero cost.

Observe `last_processing_recovery_feed` (status/run/enqueued/openBefore/hasUnlinked), phase latency, queue age, current-run links and dispositions alongside the existing capacity gates. Admission errors prevent new catch-up that pass while owned live work continues. A zero catch-up quota, disabled feeder or shadow mode performs no automatic admission. `manifest_admitted` means every member has a work link, not successful recovery. New history gaps, unavailable tokens, dead letters and final completion remain explicit.

On a stop signal, set the audited persistent pause first, let owned work settle, then disable the feeder if needed. This immediately blocks future admissions/claims without interrupting raw intake; already owned work can finish. Retain all item links, readiness evidence and accepted data. Resuming the same reviewed run continues from those links. The compatibility rollback image has no automatic feeder; recovery remains paused until an approved candidate is restored or separately bounded manual work is reviewed.

Do not enqueue the remaining catalog in a tight loop. Expand only after the full overlap cycle, with bounded progress/pause checks. Count completed, source-blocked, dead-lettered and still-unlinked members separately. Complete the run only through the existing audited completion contract after every manifest member has an honest disposition and all source-blocked outcomes are reviewed. This establishes current-state reconciliation for the manifest, never recovery of unobserved historical versions.

### PICS rollback floor

Before activation, staged records can be abandoned, retained and inspected. After activation, use pause plus forward repair. The old checkpoint rollback deletes primary work and is blocked by the new trigger. Plain baseline runtime settlement matches work IDs across old runs and must not be redeployed after successor linkage. `rollback-pics-successor-compatibility.patch` preserves current-run settlement scoping and successor pause checks on the baseline runtime. The extracted baseline source plus patch passes **161 tests**, and the exact patched class passes local successor SQL tests including pause/live claims and completion/failure/source-block audit scoping. The current Linux amd64 rollback image now builds and passes 161 offline tests; the feeder candidate passes 228. Retain the immutable image IDs and 0111, and rebuild/retest after any source or lockfile change. See `implementation-container-validation.md`; these local tests are not a production capacity measurement. No rollback may delete accepted samples, archives, batches, source watermarks or July's audit.

## Outstanding before requesting production approval

Chrome supplies authenticated page checks and real individual Vercel request durations. Vercel's aggregate Duration/TTFB charts are marked Demo Data, Speed Insights has no events, and Railway HTTP metrics still lack timing series. The user explicitly chose to skip Sentry; its access is not a prerequisite. Use the fixed low-rate direct contract probe, matched Chrome visits and real request logs described in `implementation-browser-latency-baseline.md`. These initial samples do not establish traffic-wide p95/p99 or full-overlap capacity. The post-efficiency 24-hour baseline and restored-load measurements remain required before recovery expansion.

All five architectural/sequencing choices are now approved: bounded psycopg_pool, CCU validation/tier progress checkpoints, T6 following initial rollout, the demo peak cache, and incremental YouTube calculations preserving timestamp writes. T2/E4/P1/T7 are implemented and validated locally. T7 has measured cold/full-dirty and source-write regressions that require an independent net-benefit canary. T6 remains part of the goal after residual costs are measured. See the current checklist and remaining-scope audit. The six review bundles now cover 71 source/schema files and pass baseline apply checks; current root build/types/lint pass. Rebuild/review again after source or baseline changes. The rebuilt pooling/feeder candidate now passes 228 offline tests; the unchanged compatible rollback retains its 161-test evidence. Matched latency and the post-efficiency 24-hour baseline remain rollout gates; restored-load production benefits require separately approved canaries. Compression, index deletion, server resize and embedding funding remain outside these application steps.

### T2 peak-cache rollout and rollback addition

Apply 0112 then 0114 only in an approved schema window, before deploying the new demo writer. Risk: medium, due to source trigger dependency, cold ranking costs and extra source WAL. Verify exact source trigger definitions, ordinary trigger enablement, source/table privileges, owners and the singleton row. Application uses a 3-second lock and 30-second statement bound; failure rolls back the entire migration. No source observations are rewritten. All accepted source writes/repairs must target the parent hypertables. Direct chunk DML, trigger-disabled imports, drop/detach of in-window chunks and logical-replication apply can bypass statement triggers; keep reuse disabled for such maintenance and bump the epoch after source transactions drain before re-enabling. Do not infer coverage for those administrative bypass paths from parent-table tests. Existing production compression policies stay unchanged.

Capture cold and warm rank time, source insert/update latency and WAL, per-demo generation/expiry misses, table bytes and lock waits during an overlap cycle. Compare full calculated ranks/peaks and evaluation timestamps at a shared snapshot. Do not only compare the warm pass: generated cold/fully-expired passes were about twice the prior calculation time. Initial activation and any DELETE/TRUNCATE cause a full miss; stagger them away from heavy refreshes under the existing capacity gates. Normal source updates continue to add invalidation cost even while cache reuse is disabled, preserving safe reactivation.

Immediate fallback: execute reviewed `rollback-demo-peak-cache.sql`, which waits for the evaluation row, disables reuse and bumps its epoch once; repeated execution is harmless and every source/assignment/cache remains. Candidate SQL then reads raw peaks. That alone retains the new query shape and trigger overhead. If the performance gate still fails, restore the validated assignment-only writer bundle (0112 compatibility preserved) and disable/remove the eight 0114 triggers only after all cache-aware writers are drained and reuse is disabled. Retain derived tables for inspection. Never re-enable reuse after bypassed writes without source drainage, restored exact triggers and a fresh epoch. Schema removal is optional separate reviewed cleanup, not required for runtime rollback.

### E4 progress-checkpoint rollout and rollback addition

Deploy database writer exports with the daily/tiered workers together. No migration is required. Risk: low/medium: up to 25 responses publish rotation/validation earlier, invalid skips start at the response observation time, and small batch UPDATEs add round trips. Raw samples and per-day peaks commit before progress. A failure in any phase stops new requests; a failed progress update leaves raw data retained and allows a later run to retry that app. Validate all-invalid checkpoints, midnight dates, mixed validation states and interrupted runs with retained raw rows. Compare validation age/candidate repeats and writer transaction/connection load. Returning to the previous worker retains committed samples, validations and 30-day skips; do not reset accepted progress as rollback.

### P1 bounded-pool rollout and rollback addition

Risk: medium due to persistent backend memory, additional check/reset round trips and bounded work-slot waits. Deploy the locked candidate only after SQL 0109/0111, with the existing concurrency of three. Its pools cap at six total durable connections (intake 1, work including gate 2, promotion 3); separate configured URLs still consume that combined budget across their target services. Read cumulative pool creation/request/wait/size counters alongside logical application transaction metrics. A held gate must not block intake; verify live freshness and batched lease renewal under actual overlap. Do not raise pool size, threads or quotas to hide checkout timeouts.

Each checkout owns the connection through transaction completion and synchronous reset. Closed/broken sessions are replaced; there is no automatic replay of an uncertain commit. Existing durable idempotency/lease recovery handles operation failures. Pool close rejects new work and leaves an active checkout owned until it returns. The coordinator drains its pass before closing stores. Roll back to the unchanged tested baseline-plus-successor-compatibility image if wait, memory or freshness gates fail; this removes pooling while retaining 0111/audit protection and all accepted data. No schema rollback is needed for P1. The local 100-cycle setup benchmark is not a restored-ingestion capacity claim.


### T7 incremental YouTube rollout and rollback addition

SQL 0115 must precede the incremental writer; 0109 coordination must already cover the other heavy phases. Risk: medium, because five source tables gain 20 statement triggers and publication gains derived ledger dependencies. The migration uses a 3-second lock bound and 30-second statement bound, installs atomically, clones existing output owner/ACLs, removes accidental default grants and starts with no publication date. It does not backfill or rewrite source observations. The first candidate run calculates the full requested window. Keep PICS catch-up paused for that first run; do not change the existing six-hour schedule or 2-/30-day repair inputs.

Review `implementation-youtube-incremental-summary.md` before approval. The current production planner estimates are roughly 458k videos and 435 apps. A generated 460k-video fixture passes all 25 output columns, including source/match fanout, both timestamps and three column defaults. Clean/one-app runs save substantial calculation work, but a 500-video refresh spread over all 435 apps is slower. Mature derived ledgers occupy about 93 MB in the fixture. Current collectors use individual source statements: their trigger CPU/WAL cost is measured separately and cannot be represented by the faster bulk/COPY case.

An independent canary must record normal discovery/refresh source time and WAL together with `recalculatedApps`, `publicationMs`, `elapsedIncludingGateMs`, output/source freshness, total CPU/memory, request latency and compressed-row corrections. Include a full-dirty run and a midnight/expiry cycle at unchanged cadence. Use the existing CPU/memory/latency stop gates. If the actual workload mix does not show a net benefit, retain the bounded full-query implementation and do not expand incremental use. Do not transfer predicted warm-cache savings into PICS catch-up capacity.

Immediate correctness rollback uses `rollback-youtube-incremental-rollups.sql`: disable reuse under the gate, preserve all ledgers and source/output rows, then let the candidate perform full calculations. This retains trigger overhead and is not a complete performance rollback. The concrete performance fallback is `rollback-youtube-gated-full-query.patch`, applied to the candidate source in a separately built/reviewed release. It keeps the shared gate and repeatable-read publication while using the exact validated bounded full query, with no incremental-table dependency. Its seven applicable/fallback tests and type checks pass. After reuse is disabled, `rollback-youtube-incremental-tracking.sql` removes trigger overhead using brief, bounded locks on the five source parents; no tables or observations are dropped. That step has moderate availability risk and requires its own approved window. Actual local SQL tests prove the disable guard, idempotence, unchanged output and parity after bypassed writes.

For reactivation after maintenance, review installed definitions, use `rollout-youtube-incremental-reactivate.sql` to restore tracking and clear the prior publication date under the same gate/source locks, then deploy/run the candidate through a fresh full bootstrap. Repeated unreviewed activation is rejected. Direct chunk DML, disabled-trigger imports, replication apply, in-window chunk drops/detaches, manual derived-output repairs, changed defaults/columns and calculation-version edits require this disabled/full-bootstrap discipline. Parent-table parity tests do not establish tracking for administrative bypass paths. Keep existing compression policies and all archives unchanged.
